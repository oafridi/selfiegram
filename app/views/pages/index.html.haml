%h1 Dashboard

%table
  %thead
    %tr
      %th Title
      %th Image
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= image_tag post.image.variant(resize: "100x100")
        %td= link_to 'Show', post        
%br